**ProgressBar** — thin determinate bar. Default 3px with an emerald gradient, matching the recipe-progress strip under the agent header.

```jsx
<ProgressBar value={60} />
<ProgressBar value={40} height={5} gradient={false} />
```
