**Select** — styled native dropdown for a longer list of exclusive options where a segmented control would be too wide (caption style, layout preset, language).

```jsx
<Select options={['Bold Center','Minimal Bottom','Karaoke','Boxed']} value={style} onChange={setStyle} />
```

- 36px tall, `--surface-2` fill, radius 10. Pair with a mono uppercase eyebrow label above it.
